apiVersion: v1
kind: Pod
metadata:
    name: couchdb
    labels:
      app: couchdb
spec:
    restartPolicy: Never
    containers:
    - name: pref-server-couchdb
      image: klaemo/couchdb
      ports:
          - containerPort: 5984
